# System Requirements

Define containers system requirements.

```yaml
system:
  requests:
    cpu: 1         # default
    gpu: 0         # default
    memory: 32MB   # default
  limits:
    cpu: 2         # default
    gpu: 0         # default
    memory: 64MB   # default
```
